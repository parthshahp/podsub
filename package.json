{
  "name": "podsub",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @podsub/schemas build && concurrently -n server,web -c blue,green \"pnpm --filter @podsub/server dev\" \"pnpm --filter @podsub/web dev\"",
    "build": "pnpm -r build",
    "lint": "oxlint .",
    "fmt": "oxfmt .",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0",
    "typescript": "^7.0.2"
  }
}
