# Copy to .env for local development. Never commit .env.
OPENROUTER_API_KEY=
# AnkiConnect upstream for the same-origin /api/anki proxy. Points at
# whichever machine runs Anki — loopback by default, a LAN host if Anki
# lives elsewhere.
# ANKI_CONNECT_URL=http://192.168.1.103:8765
